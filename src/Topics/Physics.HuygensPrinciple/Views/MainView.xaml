<local:MainViewBase
    x:Class="Physics.HuygensPrinciple.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Physics.HuygensPrinciple.Views"
    xmlns:Windows10version1903="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 8)"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sharedControls="using:Physics.Shared.UI.Controls" xmlns:extensions="using:Physics.Shared.Extensions" xmlns:mux="using:Microsoft.UI.Xaml.Controls"
	mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Page.Transitions>
		<TransitionCollection>
			<NavigationThemeTransition>
				<NavigationThemeTransition.DefaultNavigationTransitionInfo>
					<DrillInNavigationTransitionInfo />
				</NavigationThemeTransition.DefaultNavigationTransitionInfo>
			</NavigationThemeTransition>
		</TransitionCollection>
	</Page.Transitions>
	<Page.Resources>
		<SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="{StaticResource AppThemeColor}" />
		<SolidColorBrush x:Key="NavigationViewTopPaneBackground" Color="{StaticResource AppThemeColor}" />
		<SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="{StaticResource AppThemeColor}" />
	</Page.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MinWidth="320" />
			<ColumnDefinition Width="3*" />
		</Grid.ColumnDefinitions>
		<Grid
            x:Name="MenuPane"
            Grid.Column="0"
            RequestedTheme="Dark">
			<Grid.Background>
				<AcrylicBrush TintColor="{ThemeResource AppThemeColor}" TintOpacity="0.85" BackgroundSource="HostBackdrop" FallbackColor="{ThemeResource AppThemeColor}" />
			</Grid.Background>
			<Windows10version1903:Grid.Shadow>
				<ThemeShadow />
			</Windows10version1903:Grid.Shadow>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<sharedControls:SkewedMenuHeader FontSize="16" GoBackCommand="{x:Bind Model.GoBackCommand}" Text="{extensions:Localize Key=ShortAppName}" />

			<StackPanel Grid.Row="1" Padding="8" Margin="0,12,0,0">
				<Button
					HorizontalAlignment="Stretch"
					Command="{x:Bind Model.PickScenePresetCommand}"
					Content="{extensions:Localize Key=PickScenePreset}" />
				<CheckBox IsChecked="{x:Bind Model.RenderConfiguration.ShowWaveEdge, Mode=TwoWay}" Content="{extensions:Localize Key=ShowWaveEdge}" />
				<CheckBox IsChecked="{x:Bind Model.RenderConfiguration.ShowWave, Mode=TwoWay}" Content="{extensions:Localize Key=ShowWave}" />
				<CheckBox IsChecked="{x:Bind Model.RenderConfiguration.ShowSignificantPoints, Mode=TwoWay}" Content="{extensions:Localize Key=ShowSignificantPoints}" />
				<CheckBox IsChecked="{x:Bind Model.RenderConfiguration.ShowObject, Mode=TwoWay}" Content="{extensions:Localize Key=ShowSource}" />

				<TextBlock Text="{extensions:Localize Key=Drawing}" Style="{ThemeResource SubtitleTextBlockStyle}" />
				<ToggleSwitch IsOn="{x:Bind Model.DrawingState.IsDrawing, Mode=TwoWay}" OnContent="{extensions:Localize Key=On}" OffContent="{extensions:Localize Key=Off}" />
				<Slider Value="{x:Bind Model.DrawingState.Size, Mode=TwoWay}" Minimum="2" Maximum="20" />
				<TextBlock Text="{extensions:Localize Key=Source}" />
				<Ellipse />
			</StackPanel>
		</Grid>
		<Grid
            x:Name="SecondPane"
            Grid.Column="1"
            Padding="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<!--<InkToolbar
                x:Name="inkToolbar"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                RequestedTheme="Light"
                TargetInkCanvas="{x:Bind InkCanvas}" />-->
			<Grid x:Name="CanvasHolder" Grid.Row="1" Background="#F3F3F3" SizeChanged="CanvasHolder_SizeChanged" />
			<Canvas Grid.Row="1" PointerMoved="CanvasHolder_PointerMoved" PointerPressed="CanvasHolder_PointerMoved" PointerExited="CanvasHolder_PointerExited" x:Name="DrawingSurface" Background="Transparent" />
			<!--<InkCanvas Grid.Row="1" x:Name="InkCanvas" />-->
			
			<sharedControls:SimulationControls
                x:Name="SimulationControls"
                Grid.Row="2"
                d:Visibility="Collapsed"
				ForwardVisibility="Visible"
				BackwardVisibility="Visible"
				SpeedControlVisibility="Visible"
                IsPaused="{x:Bind Model.SimulationPlayback.IsPaused, Mode=OneWay}"
                JumpBackCommand="{x:Bind Model.SimulationPlayback.JumpBackCommand}"
                JumpForwardCommand="{x:Bind Model.SimulationPlayback.JumpForwardCommand}"
                JumpSize="{x:Bind Model.SimulationPlayback.JumpSize, Mode=TwoWay}"
                JumpToEndCommand="{x:Bind Model.SimulationPlayback.JumpToEndCommand}"
				JumpToStartCommand="{x:Bind Model.SimulationPlayback.JumpToStartCommand}"
                PauseCommand="{x:Bind Model.SimulationPlayback.PauseCommand}"
                PlayCommand="{x:Bind Model.SimulationPlayback.PlayCommand}"
                RequestedTheme="Light"
                SimulationSpeed="{x:Bind Model.SimulationPlayback.PlaybackSpeed, Mode=TwoWay}">
				<sharedControls:SimulationControls.Background>
					<AcrylicBrush
                        BackgroundSource="Backdrop"
                        FallbackColor="{ThemeResource SystemChromeMediumColor}"
                        TintColor="{ThemeResource SystemChromeMediumColor}" />
				</sharedControls:SimulationControls.Background>
			</sharedControls:SimulationControls>
		</Grid>
		<Grid Visibility="{x:Bind Model.IsLoading, Mode=OneWay}" Background="#AA000000">
			<mux:ProgressRing IsActive="{x:Bind Model.IsLoading, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50" />
		</Grid>
	</Grid>
</local:MainViewBase>
