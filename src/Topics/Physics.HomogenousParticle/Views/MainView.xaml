<views:BaseView
    xmlns:views="using:Physics.Shared.Views"
    x:Class="Physics.HomogenousParticle.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Physics.HomogenousParticle.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:conv="using:Physics.HomogenousParticle.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <Grid.Resources>
            <conv:BoolToVelocityVariantConverter x:Key="BoolToVelocityVariantConverter"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="{ThemeResource NavigationViewExpandedPaneBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="8" Grid.Row="0" x:Uid="ChooseMotion" Text="Vyber pohyb" Style="{ThemeResource TitleTextBlockStyle}"/>
            <Border Grid.Row="1" Margin="0,12,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Visible">
                    <StackPanel Margin="12,0,8,0" Orientation="Vertical">
                        <Grid>
                            <winui:RadioButtons SelectedIndex="{x:Bind Model.SelectedVariantIndex, Mode=TwoWay}">
                                <winui:RadioButtons.Resources>
                                    <Style TargetType="RadioButton">
                                        <Setter Property="Padding" Value="12,0,0,4" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                    </Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="16"></Setter>
                                    </Style>
                                </winui:RadioButtons.Resources>

                                <RadioButton>
                                    <RadioButton.Content>
                                        <TextBlock Text="v = 0"/>
                                    </RadioButton.Content>
                                </RadioButton>

                                <RadioButton>
                                    <RadioButton.Content>
                                        <TextBlock Text="v || B"/>
                                    </RadioButton.Content>
                                </RadioButton>

                                <RadioButton>
                                    <RadioButton.Content>
                                        <TextBlock Text="v &#x27c2; B"/>
                                    </RadioButton.Content>
                                </RadioButton>
                            </winui:RadioButtons>
                        </Grid>
                        <Image Margin="0,8,0,8" Width="24" Source="/Assets/arrow_down.png"></Image>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Náboj částice q" Margin="0,0,16,0"/>
                            <winui:NumberBox Margin="0,0,0,8" Grid.Row="0" Grid.Column="1" Minimum="-3" Maximum="3" IsEnabled="{x:Bind Model.IsQEnabled, Mode=OneWay}" Value="{x:Bind Model.QInput, Mode=TwoWay}" SpinButtonPlacementMode="Inline"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Orientace indukčních čar B" Margin="0,0,16,0"/>
                            <winui:NumberBox Margin="0,0,0,8" Grid.Row="1" Grid.Column="1"  Minimum="0" Maximum="360" IsEnabled="{x:Bind Model.IsBEnabled, Mode=OneWay}" Value="{x:Bind Model.OrientationInput, Mode=TwoWay}" SpinButtonPlacementMode="Inline"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0"><Run>Velikost rychlosti v 10</Run><Run Typography.Variants="Superscript">n</Run></TextBlock>
                            <winui:NumberBox x:Name="VelocityNumberBox" Margin="0,0,0,8" Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="6" ValueChanged="NumberBox_ValueChanged" IsEnabled="{x:Bind Model.IsVEnabled, Mode=OneWay}" Value="{x:Bind Model.VelocityInput, Mode=TwoWay}" SpinButtonPlacementMode="Inline"/>
                        </Grid>
                        <Button Command="{x:Bind Model.DrawCommand}" x:Uid="DrawButton" Content="Draw" HorizontalAlignment="Stretch" Margin="8,8,8,0"></Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
        <TextBlock Grid.Column="1" Text="{x:Bind Model.DrawingContent, Mode=OneWay}" FontSize="100"></TextBlock>
    </Grid>
</views:BaseView>
