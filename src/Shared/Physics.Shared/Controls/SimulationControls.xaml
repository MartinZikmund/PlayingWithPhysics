<UserControl
    x:Class="Physics.Shared.Controls.SimulationControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Physics.Shared.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Slider x:Name="TimeSlider" Visibility="{x:Bind (Visibility)IsTimeSliderVisible, Mode=OneWay}" Margin="10,0,10,0"/>
        <StackPanel HorizontalAlignment="Center" Spacing="8" Orientation="Horizontal" Grid.Row="1">
            <Button>0.5x</Button>
            <Button>2x</Button>            
            <!--<Button HorizontalAlignment="Center"  Margin="0,0,0,10">
                <SymbolIcon Symbol="Play" />
            </Button>-->
        </StackPanel>        
    </Grid>
</UserControl>
